type: vertical-stack
cards:

  # Tablet Portrait
  - type: conditional
    conditions:
      - condition: state
        entity: input_boolean.hemma_mobile_navigation
        state: "off"
      - condition: screen
        media_query: "(orientation: portrait) and (min-width: 768px) and (max-width: 1600px) and (hover: none) and (pointer: coarse)"

    card:
      type: custom:hemma-nav
      variant: tablet
      routes: &hemma_tablet_routes
        - url: /dashboard-hemma/home
          label: Home
          icon: mdi:home-variant

        - url: /dashboard-hemma/living-room
          label: Living Room
          icon: mdi:sofa
          badge:
            show: >
              [[[
                const _s = hass.states['input_text.hemma_motion_living_room']?.state?.trim();
                return hass.states['input_boolean.hemma_motion_badges']?.state === 'on'
                  && !!_s && hass.states[_s]?.state === 'on';
              ]]]
            color: '#ffd700'

        - url: /dashboard-hemma/kitchen
          label: Kitchen
          icon: mdi:fridge

        - url: /dashboard-hemma/bedroom
          label: Bedroom
          icon: mdi:bed-king
          badge:
            show: >
              [[[
                const _s = hass.states['input_text.hemma_motion_bedroom']?.state?.trim();
                return hass.states['input_boolean.hemma_motion_badges']?.state === 'on'
                  && !!_s && hass.states[_s]?.state === 'on';
              ]]]
            color: '#ffd700'

        - label: Scenes
          icon: mdi:layers
          menu: scenes

  # Tablet Landscape
  - type: conditional
    conditions:
      - condition: state
        entity: input_boolean.hemma_mobile_navigation
        state: "off"
      - condition: screen
        media_query: "(orientation: landscape) and (min-width: 600px) and (max-width: 1600px) and (pointer: coarse) and (hover: none)"

    card:
      type: custom:hemma-nav
      variant: tablet
      routes: *hemma_tablet_routes
